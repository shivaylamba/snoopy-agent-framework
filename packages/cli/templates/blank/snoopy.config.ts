// Default-export your agents array. The CLI imports this file from `agent dev`
// and `agent deploy`. Importing each agent triggers defineAgent() which
// registers with the iii engine as a side effect.
export default {
  agents: [
    // import and add your agents here
  ],
};
