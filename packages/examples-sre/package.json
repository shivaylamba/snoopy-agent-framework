{
  "name": "@snoopy/examples-sre",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "agent dev",
    "traces": "agent traces",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf .turbo"
  },
  "dependencies": {
    "@snoopy/core": "workspace:*",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@snoopy/cli": "workspace:*",
    "typescript": "^5.5.0",
    "tsx": "^4.16.0",
    "@types/node": "^20.0.0"
  }
}
