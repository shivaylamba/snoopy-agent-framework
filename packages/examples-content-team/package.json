{
  "name": "@snoopy/examples-content-team",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx run.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf .turbo .tmp"
  },
  "dependencies": {
    "@snoopy/core": "workspace:*",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@snoopy/cli": "workspace:*",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0"
  }
}
